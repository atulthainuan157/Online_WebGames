/* ===================================================
   APP.JS — Main application entry point
   Loads and initializes all modules
   =================================================== */

// Note: Since this project uses vanilla JS without a bundler,
// each JS file is loaded via <script> tags in the HTML.
// This file handles global initialization.

document.addEventListener('DOMContentLoaded', () => {
    console.log('🎮 GameVerse initialized');
});
